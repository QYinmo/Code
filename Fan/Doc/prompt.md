你现在是一位资深的嵌入式系统工程师和电子设计竞赛金牌指导老师。我正在参加“雏鹰展翅”创客命题挑战赛，赛题为《简易智能排气系统》。需要你带我完成从【器件选型】、【引脚分配与电路设计】、【完整 C 语言代码编写】到【系统调试】的全流程。

**核心约束条件：**
1. **主控芯片**：STM32F103C8T6 最小系统板。
2. **硬件要求**：控制板必须自制，要求安装简易、控制成本。
3. **电机方案**：根据赛题允许，**采用低压直流电机（如 12V）**，使用 PWM 进行无级调速控制。

**最新赛题功能与逻辑要求（必须严格遵守）：**
1. **热水检测逻辑**：判断“热水”的标准为：水温高于环境室温 5℃ 及以上（例如室温 25℃，水温 ≥30℃ 即可判定）。
2. **移动热水检测**：能对固定点、多点以及“移动的热水水源”（模拟淋浴花洒脱离底座）的开停进行检测。
3. **排气扇智能联动**：
   - 热水开启时，排气扇转速与室温呈**正相关**（室温越高，PWM 占空比越大，无级调速）。
   - 热水停止时，排气扇根据设定延时时间后自动停止。
4. **最高优先级安全保护（强制排气）**：检测到有毒气体（比赛用打火机气体模拟），**必须无条件强制排气扇以最高速（100% 占空比）持续运行，并发出明确警报**，此逻辑优先于其他所有控制！
5. **故障检测**：风扇堵转或故障自动检测。
6. **环境与交互**：
   - 检测室内温度、湿度。
   - 具备本地控制方式（按键）。
   - 具备人机交互界面（LED、OLED 显示屏、蜂鸣器），明确指示当前模式、热水状态、风扇状态、故障及气体报警信息。

---
**请你按照以下 5 个阶段，为我输出极其详细的执行方案：**

### 第一阶段：极低成本器件选型 (BOM表)
请为上述功能匹配最便宜且好用的传感器模块组合：
* **电机与驱动**：12V 直流排气扇 + 大功率 MOS 管驱动模块（如 LR7843）。
* **堵转检测**：直流电流检测模块（如 INA219 或 MAX471）。
* **多点/移动热水检测**：推荐非接触式红外测温阵列或大视场角的红外测温模块（如 MLX90614），请说明如何用它识别移动花洒。
* **其他传感器**：温湿度（DHT11/AHT20）、有毒气体（MQ-2，针对打火机丁烷气体）、OLED 显示屏（I2C）。

### 第二阶段：硬件连接、电源分配与引脚映射
* 给出一份详细的接线映射表（STM32 的哪个引脚接哪个模块）。
* **电源方案极其重要**：系统有 12V 动力和 5V/3.3V 逻辑，请设计可靠的供电分配方案（12V 适配器 + LM2596S 降压模块）。
* 合理分配 ADC（用于气体和电流读取）、I2C（用于 OLED 和红外测温）、定时器（PWM 输出）和外部中断（按键）。

### 第三阶段：核心代码编写 (基于 HAL 库)
请为我生成完整的工程逻辑代码（C语言）。不需要生成所有的工程样板文件，但必须提供：
1. `main.c` 中的核心初始化和 `while(1)` 轮询控制逻辑。
2. **状态机控制逻辑**：严格实现赛题要求的逻辑优先级（气体报警最高优先级全速运行 -> 堵转故障急停 -> 热水温差联动调速 -> 延时停机）。
3. **重点算法 1**：如何比较红外测温值与环境室温，动态计算 `T_water >= T_ambient + 5` 的阈值，并映射为 PWM 占空比？
4. **重点算法 2**：堵转检测的电流阈值判断逻辑。

### 第四阶段：电路自制与搭建注意事项
* 指导如何自制控制板（洞洞板焊接），处理 12V 动力线与 3.3V 信号线时的安全/隔离/抗干扰注意事项（反电动势处理）。

### 第五阶段：调试指南与测试方案
* 针对赛题的测试要求（如用一杯冷热水模拟水循环、用打火机模拟气体），给出分步联调测试步骤。